# Dependencies — installed fresh inside the container
node_modules/

# Vite build output — rebuilt inside the container
dist/
dist-ssr/
.vite/

# Environment files — injected at build time via --build-arg
.env
.env.*
!.env.example

# Editor & OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Git
.git/
.gitignore
